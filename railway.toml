[railway]
  builder = "docker"
  dockerfilePath = "Dockerfile"

[railway.variables]
  NODE_ENV = "production"
  PORT = "8080"

[deploy]
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 10